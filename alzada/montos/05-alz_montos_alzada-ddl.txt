-- OBTENER LAS OCURRENCIAS
select distinct d.*
from (SELECT C_Nro_Expediente + ' ' as result
      FROM TSS_MONTOS
      where C_Nro_Expediente like 'ARIT-BEN-%'
      group by C_Nro_Expediente
     ) as d
order by d.result;

where C_Nro_Expediente like 'ARIT-CBA-%'
where C_Nro_Expediente like 'ARIT-CHQ-%'
where C_Nro_Expediente like 'ARIT-LPZ-%'
where C_Nro_Expediente like 'ARIT-ORU-%'
where C_Nro_Expediente like 'ARIT-PND-%'
where C_Nro_Expediente like 'ARIT-PTS-%'
where C_Nro_Expediente like 'ARIT-SCZ-%'
where C_Nro_Expediente like 'ARIT-TJA-%'

WHEN LTRIM(RTRIM(ra.C_Nro_Expediente))

-- TOTAL
SELECT COUNT(*)
FROM TSS_MONTOS AS ra
WHERE C_Nro_Expediente LIKE 'ARIT-BEN-%';

-- CONSULTA, PEGAR LAS OCURRENCIAS, hacer pruebas con limit, si es necesario, HABER
select (select ra.numero_recurso_alzada
        from alzada.alz_recursos_alzada ra
        where ra.id = temp.id_recurso_alzada) as numero_recurso_alzada,
       temp.id as id
from (select id_recurso_alzada, id
      from alzada.alz_actos_impugnados
      where id_recurso_alzada in (
          select id
          from alzada.alz_recursos_alzada
          where numero_recurso_alzada in
                (
                    ))) AS temp
order by numero_recurso_alzada;

limit 1000 offset 0;
-- limit 1000 offset 1000;
-- limit 1000 offset 2000;
-- limit 1000 offset 3000;
-- limit 1000 offset 4000;
-- limit 1000 offset 5000;

TOTAL

SELECT count(C_Nro_Expediente)
FROM TSS_MONTOS
WHERE (C_Nro_Expediente LIKE 'ARIT-%/2021%' OR C_Nro_Expediente like 'ARIT-%/2022%');